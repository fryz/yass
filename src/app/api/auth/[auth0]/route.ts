/**
 * Auth0 v4 handles authentication routes via middleware.
 * The /auth/* routes (login, logout, callback) are intercepted by auth0.middleware()
 * in src/middleware.ts — no separate route handler is needed.
 *
 * This file is kept as a placeholder to ensure the directory exists for future
 * custom auth route extensions if needed.
 */
export {};
